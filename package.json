{
  "name": "derrickmo-website",
  "private": true,
  "version": "0.1.0",
  "description": "Derrick Mo — personal website (React, Vite static build, GitHub Pages)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate": "node validate-insights.mjs && node scripts/validate-content.mjs && node scripts/gen-from-store.mjs",
    "prebuild": "npm run validate",
    "build": "vite build",
    "preview": "vite preview",
    "content": "node scripts/gen-from-store.mjs --write",
    "audit": "node scripts/audit-curriculum.mjs && node scripts/check-page-assets.mjs && node scripts/check-notebook-sync.mjs && node scripts/add-cdn-sri.mjs --check"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  }
}
